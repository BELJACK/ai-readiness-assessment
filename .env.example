# Postgres connection string. Any Postgres works; Neon is what this was built against.
# Vercel: add this in Project Settings > Environment Variables.
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
